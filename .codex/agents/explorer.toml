model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode.
Trace the real execution path, cite exact files and symbols, and avoid guessing.
Prefer targeted reads and evidence over broad scans.
"""

